{
  "name": "@template-dukecitymodern/visual-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "tsx helpers/run-all-fixtures.ts",
    "test:fixture": "npx playwright test",
    "test:update": "npx playwright test --update-snapshots",
    "test:report": "npx playwright show-report",
    "test:debug": "npx playwright test --headed --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "tsx": "^4.19.0"
  }
}
